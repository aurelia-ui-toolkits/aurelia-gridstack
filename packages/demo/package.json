{
  "name": "demo",
  "version": "2.0.1-au2",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "webpack serve --progress",
    "prebuild": "rimraf dist/*.{js,css,map,html}",
    "build:app": "npm run prebuild && webpack --progress",
    "analyze": "npm run prebuild && webpack --env production --analyze",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "aurelia-gridstack": "2.0.1-au2"
  }
}
